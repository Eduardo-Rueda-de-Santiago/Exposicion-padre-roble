[project]
name = "exposicion-padre-roble"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.136.1",
    "flask>=3.1.3",
    "flask-cors>=6.0.2",
    "pyserial>=3.5",
    "simpleaudio>=1.0.4",
    "uvicorn>=0.46.0",
]

[tool.uv.workspace]
members = [
    "examples/song_overlap_python",
]
